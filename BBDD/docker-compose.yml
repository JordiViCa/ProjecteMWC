# Use root/example as user/password credentials
version: '3.1'

services:

  mongo:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - /data/db:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: adminAdvenaCat
      MONGO_INITDB_ROOT_PASSWORD: jpi%bfOk5T@1Bx1iD%2ZUmZ2RjV#Qz*jof%xtMVei11R7pMdYQ
